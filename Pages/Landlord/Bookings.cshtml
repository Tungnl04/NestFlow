@page
@model NestFlow.Pages.Landlord.BookingsModel
@{
    ViewData["Title"] = "Quản lý Booking - NestFlow";
    Layout = "~/Pages/Shared/_LandlordLayout.cshtml";
}

<div class="container-fluid py-4">
    <div class="row mb-4">
        <div class="col-12">
            <h2 class="mb-0">
                <i class="fas fa-calendar-check me-2"></i>
                Quản lý Booking
            </h2>
            <p class="text-muted">Xem và xử lý các yêu cầu đặt cọc phòng</p>
        </div>
    </div>

    <!-- Tabs -->
    <ul class="nav nav-tabs mb-4" id="bookingTabs" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" id="pending-tab" data-bs-toggle="tab" data-bs-target="#pending" type="button">
                <i class="fas fa-clock me-2"></i>
                Chờ thanh toán
                @if (Model.PendingBookings.Any())
                {
                    <span class="badge bg-warning ms-2">@Model.PendingBookings.Count</span>
                }
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="confirmed-tab" data-bs-toggle="tab" data-bs-target="#confirmed" type="button">
                <i class="fas fa-check-circle me-2"></i>
                Đã chấp nhận
                @if (Model.ConfirmedBookings.Any())
                {
                    <span class="badge bg-success ms-2">@Model.ConfirmedBookings.Count</span>
                }
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="rejected-tab" data-bs-toggle="tab" data-bs-target="#rejected" type="button">
                <i class="fas fa-times-circle me-2"></i>
                Đã từ chối
                @if (Model.RejectedBookings.Any())
                {
                    <span class="badge bg-danger ms-2">@Model.RejectedBookings.Count</span>
                }
            </button>
        </li>
    </ul>

    <!-- Tab Content -->
    <div class="tab-content" id="bookingTabsContent">
        <!-- Pending Bookings -->
        <div class="tab-pane fade show active" id="pending" role="tabpanel">
            @if (!Model.PendingBookings.Any())
            {
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Không có booking nào đang chờ thanh toán
                </div>
            }
            else
            {
                <div class="row">
                    @foreach (var booking in Model.PendingBookings)
                    {
                        <div class="col-md-6 col-lg-4 mb-4">
                            @await Html.PartialAsync("_BookingCard", booking)
                        </div>
                    }
                </div>
            }
        </div>

        <!-- Confirmed Bookings -->
        <div class="tab-pane fade" id="confirmed" role="tabpanel">
            @if (!Model.ConfirmedBookings.Any())
            {
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Chưa có booking nào được chấp nhận
                </div>
            }
            else
            {
                <div class="row">
                    @foreach (var booking in Model.ConfirmedBookings)
                    {
                        <div class="col-md-6 col-lg-4 mb-4">
                            @await Html.PartialAsync("_BookingCard", booking)
                        </div>
                    }
                </div>
            }
        </div>

        <!-- Rejected Bookings -->
        <div class="tab-pane fade" id="rejected" role="tabpanel">
            @if (!Model.RejectedBookings.Any())
            {
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Chưa có booking nào bị từ chối
                </div>
            }
            else
            {
                <div class="row">
                    @foreach (var booking in Model.RejectedBookings)
                    {
                        <div class="col-md-6 col-lg-4 mb-4">
                            @await Html.PartialAsync("_BookingCard", booking)
                        </div>
                    }
                </div>
            }
        </div>
    </div>
</div>

 
